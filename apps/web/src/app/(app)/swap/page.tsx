import { SwapContent } from './_components/swap-content';

export const metadata = {
  title: 'Swap | JakartAgents — Powered by AI',
};

export default function SwapPage() {
  return <SwapContent />;
}
